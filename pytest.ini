[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    integration: testes de integração entre scraping, parsing e persistência
    validation: cenários de validação da orquestração e dos entregáveis
